name: auto-update-cl1560

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */1 * * *"   # اجرای دقیق هر 12 ساعت

permissions:
  contents: write

jobs:
  cl-job:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11

      - name: Install dependencies
        run: |
          pip install requests psutil retrying PyYAML

      - name: Run cl1560.py
        run: |
          echo "🚀 Running cl1560.py ..."
          timeout 240s python cl1560.py || echo "❌ Script crashed"

      - name: Commit & Push updated outputs
        run: |
          echo "📌 Checking output files..."
          if [[ -s final1560.txt || -s normal1560.txt ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add final1560.txt normal1560.txt
            git commit -m "auto update (cl1560) [skip ci]" || echo "No changes"
            git push || echo "❌ Push failed"
          else
            echo "⚠️ Output files missing or empty"
          fi
